import React from 'react';
import LoginButton from './LoginButton';

const Landing = () => {
  return (
    <div>
      <p>LANDING: Sign in to hear what's in Limbo</p>
      <LoginButton></LoginButton>
    </div>
  )
};

export default Landing;